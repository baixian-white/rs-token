# E41 — direct-quantize-RemoteCLIP (DCRC) control, seed 43.

run_name: e41_direct_clip_s43
seed: 43

data:
  splits_dir: D:/CODE/遥感+通信/遥感+通信/rstoken/data/AID_splits_local
  image_size: 256
  batch_size: 16
  num_workers: 4
  pin_memory: true

model:
  teacher_ckpt: D:/CODE/遥感+通信/遥感+通信/rstoken/checkpoints/remoteclip/RemoteCLIP-ViT-B-32.pt
  teacher_name: ViT-B-32
  teacher_dim: 512
  grid_size: 16
  token_dim: 8
  codebook_size: 1024
  rvq_num_quantizers: 4
  commitment_weight: 0.25
  base_channels: 64
  channel_multipliers: [1, 2, 4, 4]
  num_res_blocks: 1
  out_channels: 3

optim:
  lr: 1.0e-4
  weight_decay: 0.0
  betas: [0.9, 0.95]
  warmup_steps: 500
  total_epochs: 50
  grad_clip: 1.0

loss:
  l1_weight: 1.0
  lpips_weight: 0.1
  vq_weight: 1.0
  lpips_net: alex

logging:
  log_every: 50
  val_every_epoch: 1
  save_every_epoch: 5
  num_vis_samples: 8
  use_wandb: false
  wandb_project: rstoken
  ckpt_dir: D:/CODE/遥感+通信/遥感+通信/rstoken/checkpoints/paper_v05/e41_direct_clip_s43
  log_dir:  D:/CODE/遥感+通信/遥感+通信/rstoken/logs/paper_v05/e41_direct_clip_s43

device: cuda
amp: true
amp_dtype: bf16
